# Backend API base URL (no trailing slash)
# In development this is proxied by Vite — leave empty or set to http://localhost:3000
# In production (Cloudflare Pages) set this to your deployed backend URL
# e.g. https://api.schemaweaver.io
VITE_API_BASE_URL=
